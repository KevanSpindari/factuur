{% extends 'base.html.twig' %}

{% block title %}Table{% endblock %}

{% block body %}
    <h1>Table</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ table.id }}</td>
            </tr>
            <tr>
                <th>Klantnaam</th>
                <td>{{ table.klantnaam }}</td>
            </tr>
            <tr>
                <th>Btwnummer</th>
                <td>{{ table.btwnummer }}</td>
            </tr>
            <tr>
                <th>Adres</th>
                <td>{{ table.adres }}</td>
            </tr>
            <tr>
                <th>Plaats</th>
                <td>{{ table.plaats }}</td>
            </tr>
            <tr>
                <th>Postcode</th>
                <td>{{ table.postcode }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('table_index') }}">back to list</a>

    <a href="{{ path('table_edit', {'id': table.id}) }}">edit</a>

    {{ include('table/_delete_form.html.twig') }}
{% endblock %}
